name: Wildberries to Supabase Sync (OLD JS) - DISABLED

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Stop
        run: echo "Old JS sync is disabled. Use wb_orders_sync.yml (Python) instead."
